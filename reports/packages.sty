\usepackage[utf8]{inputenc}
\usepackage[english]{babel}
    \addto\captionsenglish{\renewcommand{\contentsname}{Table of Contents}}
\usepackage{amsmath}

\usepackage[a4paper,lmargin={3cm},rmargin={3cm},tmargin={3cm},bmargin         ={3cm}]{geometry}
\usepackage{setspace}
 \renewcommand{\baselinestretch}{1.5}
\pagenumbering{roman}
\setlength{\arrayrulewidth}{0.5mm} 
\setlength{\tabcolsep}{5mm}
\renewcommand{\arraystretch}{1.2}
\usepackage{booktabs}
\usepackage[ddmmyyyy]{datetime}
\renewcommand{\dateseparator}{.}
\setlength{\headheight}{14.5pt}
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\rhead{\thepage}
\lhead{\nouppercase\leftmark}
\usepackage{minted} 

\usepackage[backend=biber,style=apa,sorting=nyt]{biblatex}
\addbibresource{2_references.bib}
    %calls the bib file which should be used
\usepackage{csquotes}
    %recommended to load when using biblatex
    %changes the font of the author in the bibliography
    %the {#1} specifies in which position the to-be-changed item is located
\AtEveryBibitem{\clearfield{doi}}
\AtEveryBibitem{\clearfield{url}}
    %hides the doi and url from the bibliography, even if it is included in the bib file and the citation style actually includes the doi and/or url

\usepackage[toc, page]{appendix}
    %needed for the appendix
\usepackage[raggedright]{titlesec}
    %needed since otherwise the title of section 2 exceeds the text margins

\usepackage{hyperref}   % Hyper-references, possible to change color
\hypersetup{    % Color of hyper-references
    colorlinks,
    citecolor = black,
    filecolor = black,
    linkcolor = black,
    urlcolor = black
}

\usepackage[nogroupskip]{glossaries}
    %the nogroupskip removes the grouping by the first letter of the abbreviation
\setlength{\glsdescwidth}{11cm}
    %sets the width of the glossary

\usepackage[nogroupskip]{glossaries}

\usepackage{pgfplots}
\usepackage{animate}
\usepackage{graphicx}
